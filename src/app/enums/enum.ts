export enum FormControls {
  firstName = 'firstName',
  lastName = 'lastName',
  chartNo = 'chartNo',
  address = 'address',
}

export interface Customer {
  //   id?: number;
  name?: string;
  lastname?: string;
  chart_no?: string;
  address?: any;
}
